# AI電気施工アシスタント Project Start

このファイルは、新しいGPTチャットを開始するときに、そのまま貼って使う統合引継ぎファイルである。

--------------------------------

## 回答方針

1. 無駄な説明は禁止
2. まず結論
3. 次に実行プロンプト
4. Cursor用プロンプトはそのまま貼れる形式
5. 変更は必ず1ファイル単位
6. 最小差分
7. 既存ロジック破壊禁止
8. commit / push を含める
9. 危険コマンドは提案しない
10. 良い提案や必要条件や注意点は最初の回答でまとめて出す
11. 後から「もう一つ」「追加でこれも」は原則しない

--------------------------------

## GPT回答フォーマット

1 現状評価
2 次にやるべき1手
3 Cursorに投げるプロンプト
4 次のステップ

--------------------------------

## Cursor実装報告フォーマット

変更したファイル一覧

追加した関数一覧

修正した内容

影響範囲

受け入れテスト結果

--------------------------------

## Cursor用プロンプト必須項目

変更ファイル
追加関数
仕様
テスト
git
作業ログ

--------------------------------

## 禁止コマンド

rm
del
sudo
git reset --hard
git clean
git checkout -- .
git restore .
git reset --mixed
git reset --soft

--------------------------------

## Allowlistコマンド

git status
git add
git commit
git push
git diff
git log
ls
pwd
Get-ChildItem
Get-Location

--------------------------------

## 変更禁止関数

generateDiagram()
groupDevicesByControl()
judgeSleeve()

--------------------------------

## エンジン構造（変更禁止）

parser
groups
circuits
connectionPoints
graph
layout
wirePaths
SVG

--------------------------------

## 現在フェーズ

複線図エンジン安定化

--------------------------------

## 現在の状態

・複線図生成 正常
・3路生成 正常
・材料一覧UI 実装済み
・回路別材料一覧 表示実装済み
・ガミデンキモード 実装済み
・Engine Layer 保護ルール rules.md に追加済み
・parser / groups / circuits / connectionPoints / graph は変更禁止
・layout / wirePaths / SVG はレイアウト調整のみ許可

--------------------------------

## 現在の実装前提

材料一覧は簡略材料計算
現場用区間材料計算は未実装

--------------------------------

## 次にやること

複線図エンジン安定化テスト

テスト対象

・片切1灯
・片切2灯
・3路1灯
・3路2灯
・照明＋コンセント
・コンセントのみ

--------------------------------

## 新チャットでの使い方

新しいGPTチャットでは、このファイル全文を貼り、

「以下を前提に回答してください」

とだけ伝える。

--------------------------------
