# AI電気施工アシスタント Project Start

このファイルは、新しいGPTチャットを開始するときに、そのまま貼って使う統合引継ぎファイルである。

--------------------------------

## 回答方針

1. 無駄な説明は禁止
2. まず結論
3. 次に実行プロンプト
4. Cursor用プロンプトはそのまま貼れる形式
5. 変更は必ず1ファイル単位
6. 最小差分
7. 既存ロジック破壊禁止
8. commit / push を含める
9. 危険コマンドは提案しない
10. 良い提案や必要条件や注意点は最初の回答でまとめて出す
11. 後から「もう一つ」「追加でこれも」は原則しない

--------------------------------

## GPT回答フォーマット

1 現状評価
2 次にやるべき1手
3 Cursorに投げるプロンプト
4 次のステップ

--------------------------------

## Cursor実装報告フォーマット

変更したファイル一覧

追加した関数一覧

修正した内容

影響範囲

受け入れテスト結果

--------------------------------

## Cursor用プロンプト必須項目

変更ファイル
追加関数
仕様
テスト
git
作業ログ

--------------------------------

## 禁止コマンド

rm
del
sudo
git reset --hard
git clean
git checkout -- .
git restore .
git reset --mixed
git reset --soft

--------------------------------

## Allowlistコマンド

git status
git add
git commit
git push
git diff
git log
ls
pwd
Get-ChildItem
Get-Location

--------------------------------

## 変更禁止関数

generateDiagram()
groupDevicesByControl()
judgeSleeve()

--------------------------------

## エンジン構造（変更禁止）

parser
groups
circuits
connectionPoints
graph
layout
wirePaths
SVG

--------------------------------

## 現在フェーズ

複線図エンジン安定化

--------------------------------

## 現在の状態

・複線図生成 正常
・3路生成 正常
・材料生成（簡略版） 正常
・UI同期 正常
・Engine Layer 保護ルール追加済み

--------------------------------

## 現在の実装前提

材料一覧は現在

簡略材料計算

であり

現場用の区間材料計算は未実装。

現在の材料一覧は回路単位の簡略表示であり、現場で使う最終材料数とは一致しない場合がある。

--------------------------------

## 次にやること

複線図エンジン安定化

対象テスト

・片切1灯
・片切2灯
・3路1灯
・3路2灯
・照明＋コンセント
・コンセントのみ

--------------------------------

## 新チャットでの使い方

新しいGPTチャットでは、このファイル全文を貼り、

「以下を前提に回答してください」

とだけ伝える。

--------------------------------

テスト

新規ドキュメント作成のみ

--------------------------------

git

git status
git add PROJECT_START.md
git commit -m "docs: add unified project start file"
git push

--------------------------------

作業ログ

【2026-03-08 作業終了時点】

本日やったこと

・統合引継ぎファイル PROJECT_START.md 作成

現在の状態

・新チャット用の統合引継ぎ方式へ移行

次回やること

・複線図エンジン安定化
